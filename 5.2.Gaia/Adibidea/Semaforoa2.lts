const Max = 3
range Int = 0..Max
P = (s.behera->kritikoa->s.gora->P).
SEMAFOROA(N=0) = SEMA[N],
SEMA[b:Int] = ( gora ->SEMA[b+1]
 |when(b>0) behera->SEMA[b-1]
).

||SEMADEMO = ( p[1..3]:P || {p[1..3]}::s:SEMAFOROA(1)).
